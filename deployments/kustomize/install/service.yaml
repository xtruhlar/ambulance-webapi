kind: Service
apiVersion: v1
metadata:
  name: xdt-ambulance-webapi
spec:
  selector:
    pod: xdt-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port